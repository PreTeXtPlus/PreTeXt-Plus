<% content_for(:title, @term_title) %>
<% content_for(:heading, @term_title) %>
<% content_for :subtitle do %>
  Last updated on
  <%= local_time_tag(@term.updated_at, date_only: true) %>
<% end %>

<div class="prose prose-slate mt-4 max-w-none">
  <%= render_markdown(@term.content) %>
</div>
