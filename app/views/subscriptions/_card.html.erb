<div class="rounded-lg border border-gray-100 bg-white p-4 shadow-sm transition hover:shadow-lg sm:p-6">
    <div class="text-gray-600 mb-4">
        You are currently subscribed to the
        <span class="font-semibold text-pink-600">
            <%= subscription.type.name %>
        </span> plan.
        </div>
        <div class="text-left mb-4">
        <p class="text-gray-600">
            <span class="font-semibold">Renewal Date:</span>
            <%= subscription.current_period_end.strftime('%B %d, %Y') %>
        </p>
        <p class="text-gray-600">
            <span class="font-semibold">Price:</span>
            <span class="text-pink-600">
            <%= subscription.type.price %>/<%= subscription.type.recurrence %>
            </span>
        </p>
    </div>
</div>
