<% content_for :title, "Edit announcement" %>
<% content_for :heading, "Edit announcement" %>
<% content_for :actions do %>
  <%= link_to "Back", admin_announcement_path(@announcement), class: "rounded-md px-3.5 py-2 bg-gray-100 hover:bg-gray-50 text-gray-800 font-medium" %>
<% end %>

<div class="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
  <%= render "form", announcement: @announcement %>
</div>
